No code changes were needed this iteration: the fix was already committed in `bb3596cd`, and I've now re-checked it on this branch.

**What changed:**
- The hygiene scan's `hardcoded` count now counts numbers used as limits in comparisons, like `[[ $n -ge 3 ]]` or `(( n > 5 ))`. It no longer counts the word "hardcoded".
- The scan skips comments, test files, exit-code checks and argument-count checks.
- It adds two new counts: `literal_defaults` for `${VAR:-N}` defaults, and `hardcoded_mentions`, which keeps the old word count.
- Test suites can now point the scan at a scratch folder (`SW_HYGIENE_REPO_DIR`) so they don't write into the real `.claude/`.
- The strategic agent's summary now includes `literal_defaults`.

**Checks run this iteration:**
- **Hygiene:** 43/43 passed. This includes the new tests showing that adding the word "hardcoded" in a comment doesn't change the count, and that replacing `-ge 3` with a config value lowers it.
- **Strategic:** 30/30 passed.
- **Doctor:** every check passed, but the summary line says "All 0 tests passed". The pass counter was already broken before this fix; I ran the older code to confirm.
- **Loop:** 74/74 passed. Last iteration's "Loop stuckness detection" failure didn't come back on this run and none of the fix's files are in the loop code, so it looks like a flaky test.
- **Real repo, scanned from a copy:** `hardcoded` went from 49 to 567. Separately, `literal_defaults` is 151 and `hardcoded_mentions` is 64. The sample list holds 25 entries (15 numbers, 10 markers). The lines named in the plan, such as `lib/session-restart.sh:215` and `:350`, contain the kind of comparison it now counts, but I didn't check them against the scan output.

I didn't run the full `npm test` this iteration.

The previous audit's `--json-schema` error came from the loop tooling, not this code, so there was nothing here to fix for it.

One thing that doesn't match: this branch is `docs/e2e-test-add-comment-to-readme-automated-6231`, not the plan's `fix/fix-hygiene-scan-false-positives-on-the-5846`. So the hygiene fix is sitting on a branch with an unrelated README commit.

LOOP_COMPLETE
